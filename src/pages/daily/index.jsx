import { useState } from "react";

const Daily = (props) => {
    const {date, forecastday} = props;
    console.log(date, forecastday);

    return  (
        <h1>Hola</h1>
    )
}

export default Daily;